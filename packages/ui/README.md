# Shared UI Boundary

Reusable design tokens and resource components will move here after the Windows client begins. The current Web implementation uses the existing CSS variables and Lucide icon system so that extraction remains mechanical.
