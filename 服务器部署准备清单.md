# 服务器部署准备清单

> 当前项目仍按个人本机使用设计；真正部署到公网服务器前，必须先补登录、权限和 HTTPS。本文只做部署准备清单，不在当前阶段实现登录或多用户权限。

## 1. 部署前必须确认

- 确认服务器只开放必要端口：HTTP/HTTPS、SSH，MySQL 不直接暴露公网。
- 确认已经准备域名和 HTTPS 证书，推荐使用 Nginx/Caddy 反向代理到后端 `3000`。
- 确认 `.env` 不会提交到代码仓库，生产环境单独维护。
- 确认 `SESSION_SECRET`、`MCP_TOKEN`、数据库密码、AI Key 都换成强随机值。
- 确认 `AUTH_MODE` 在公网部署前不再是 `disabled`。
- 确认 `AUTH_SECURE_COOKIES=true`，并且站点通过 HTTPS 访问。
- 确认 `CORS_ALLOWED_ORIGINS` 只填写正式域名，不保留不需要的本地地址。
- 确认上传目录或对象存储有备份策略。

## 2. 推荐生产配置

### 基础服务

```bash
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=assistant_task_board
DB_PASSWORD=replace-with-strong-password
DB_DATABASE=assistant_task_board
```

### 登录与权限

```bash
AUTH_MODE=oidc
SESSION_SECRET=replace-with-long-random-secret
AUTH_SECURE_COOKIES=true
OIDC_ISSUER=https://issuer.example.com
OIDC_CLIENT_ID=assistant-task-board
OIDC_CLIENT_SECRET=replace-me
OIDC_REDIRECT_URI=https://tasks.example.com/auth/callback
OIDC_SCOPE=openid profile email
OIDC_ALLOWED_SUBJECTS=
```

说明：

- 个人使用可把 `OIDC_ALLOWED_SUBJECTS` 设置为自己的账号 subject，避免其他同租户账号进入。
- 如果暂时没有 OIDC，不建议直接公网开放；应使用 VPN、内网穿透访问控制或服务器基础认证临时保护。

### AI / OCR / LlamaIndex

```bash
LITELLM_BASE_URL=https://your-ai-gateway.example.com/v1
LITELLM_API_KEY=replace-me
LITELLM_CHAT_MODEL=mimo-v2.5-pro
LITELLM_EMBEDDING_MODEL=replace-with-embedding-model

OCR_BASE_URL=
OCR_API_KEY=
OCR_MODEL=
OCR_MAX_PDF_PAGES=20

AI_INDEXING_ENABLED=true
QDRANT_URL=http://127.0.0.1:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=assistant_task_board
```

说明：

- OCR 模型如果和对话模型不是同一个网关，应单独设置 `OCR_BASE_URL / OCR_API_KEY / OCR_MODEL`。
- `AI_INDEXING_ENABLED=true` 后需要同时运行 `npm run ai:worker`。
- 如果服务器资源有限，先保持 `AI_INDEXING_ENABLED=false`，让数据库兜底检索继续工作。

### 附件存储

本机单人服务器可先使用：

```bash
STORAGE_DRIVER=local
STORAGE_LOCAL_ROOT=/srv/assistant-task-board/uploads
```

更推荐生产长期使用对象存储：

```bash
STORAGE_DRIVER=s3
S3_ENDPOINT=https://s3.example.com
S3_REGION=us-east-1
S3_BUCKET=assistant-task-board
S3_ACCESS_KEY_ID=replace-me
S3_SECRET_ACCESS_KEY=replace-me
S3_FORCE_PATH_STYLE=true
```

## 3. 首次部署流程

1. 拉取代码并安装依赖：

```bash
npm ci
```

2. 准备 `.env`：

```bash
cp .env.example .env
```

3. 修改 `.env` 中的数据库、登录、AI、附件存储配置。

4. 初始化数据库：

```bash
npm run init-db
```

5. 构建前端：

```bash
npm run build
```

6. 启动服务：

```bash
npm run start
```

7. 如果启用向量索引，另启 worker：

```bash
npm run ai:worker
```

8. 配置反向代理，把正式域名转发到 `127.0.0.1:3000`。

## 4. 上线前检查

```bash
npm run check:system -- --json
npm run smoke
npm run smoke:mcp
npm run smoke:attachments
```

如果启用了向量索引：

```bash
npm run smoke:llamaindex
npm run smoke:index-queue
```

如果接入 OpenClaw 写入动作：

```bash
npm run smoke:mcp-actions
```

## 5. 备份与恢复

创建备份：

```bash
npm run backup:create
```

校验最近备份：

```bash
npm run backup:verify
```

恢复预演：

```bash
npm run backup:restore
```

真正恢复必须显式确认：

```bash
npm run backup:restore -- <backup-dir> --apply --yes
```

注意：

- 恢复会覆盖 MySQL 数据，并替换 uploads 目录。
- 恢复前当前 uploads 会被重命名为安全副本。
- 建议先在测试服务器恢复验证，再对生产数据执行。

## 6. OpenClaw 接入前置条件

- 必须启用 HTTPS。
- 必须启用登录与权限。
- 必须设置强随机 `MCP_TOKEN`。
- `MCP_ENABLED=true` 前先运行 `npm run smoke:mcp`。
- 写入类动作必须继续走审批队列，不允许外部智能体直接修改正式数据。
- 审批记录和操作结果需要定期备份。

## 7. 上线后日常运维

- 每天或每周执行一次 `backup:create`，并定期执行 `backup:verify`。
- 每次升级前先备份。
- 每次修改 `.env` 后运行 `check:system`。
- 定期检查附件中心和系统页，确认附件数量、备份状态和 OCR 状态正常。
- 发现 AI 回答引用异常时，先检查 OCR 文本缓存、索引 worker 和 Qdrant 状态。

## 8. 暂不实现项

- 当前阶段不实现登录页面、多用户角色、团队权限。
- 当前阶段不把恢复按钮放进页面，避免误覆盖。
- 当前阶段不开放公网 MCP 写入，直到登录、HTTPS、强 token 和审批链路都完成。
