# 优化执行记录

## 第 1 步：体验与界面清理

### 第一阶段：乱码与占位提示清理

**计划比较**

- 方案 A：一次性重构所有界面文案、布局和移动端样式。覆盖面最大，但风险高，容易影响已有功能。
- 方案 B：先清理最显眼的乱码、英文占位和 Toast 提示，再继续做视觉统一。风险较低，验证明确。
- 方案 C：只修用户截图中出现的问题。速度最快，但不能系统性改善体验。

**选择方案**

选择方案 B。先处理高频、用户可见、可验证的问题，保持功能逻辑不变。

**实施内容**

- 将前端 Toast 标题从 `Error / Done / Notice` 统一为 `出错了 / 完成 / 提示`。
- 将英文“操作完成 / 操作失败”等占位提示替换为自然中文。
- 修复主题切换、上传附件、任务进度、附件识别相关的残留英文或异常文案。
- 清理本阶段扫描到的问号占位显示。
- 检查核心文件中的典型 mojibake 字符，确认当前核心文件未残留常见乱码。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 文本扫描：核心文件未发现本阶段目标占位文案和典型乱码字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

### 第二阶段：视觉一致性优化

**计划比较**

- 方案 A：重新设计 AI 页面、来源区、富文本预览和附件系统的完整组件结构。效果上限高，但改动大，容易影响已有交互。
- 方案 B：保持现有组件结构，统一卡片层级、间距、状态徽标、悬停反馈和移动端换行规则。风险可控，能直接修复当前观感问题。
- 方案 C：只改 AI 来源区。能解决局部问题，但笔记、附件、AI 输出仍会显得割裂。

**选择方案**

选择方案 B。当前核心问题是视觉不统一和移动端挤压，先通过样式系统化收敛，后续再进入功能增强。

**实施内容**

- 优化 AI 来源卡片：增加轻量左侧识别条、标题截断、摘要两行收起和统一悬停反馈。
- 优化 AI 对话身份显示，将用户气泡角色从英文 `You` 改为中文 `我`。
- 优化富文本笔记预览：统一标题、列表、链接、行高和行内代码的阅读样式。
- 优化附件卡片：统一 hover、预览图标、文件图标、OCR 状态徽标和按钮换行。
- 优化窄屏显示：来源按钮、附件卡片和附件操作按钮在手机宽度下不再挤成竖排。
- 修复附件 OCR 重试按钮文案，将 `Processing / Retry` 改为 `识别中 / 重试`。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 文本扫描：核心文件未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续执行第 2 步“今日工作台”。建议先做只读信息聚合页面，再逐步加入快捷操作入口，避免一开始就把任务、日志、笔记和 AI 的写入行为混在一起。

## 第 2 步：今日工作台

### 第一阶段：只读聚合页与快捷入口

**计划比较**

- 方案 A：完全前端聚合，复用任务、笔记、汇总等现有接口。改动少，但拿不到全局今日附件记录，日志也需要按任务逐个请求，数据不完整且请求多。
- 方案 B：新增一个只读聚合接口 `/api/workbench`，后端一次返回日期范围内的任务、日志、笔记和附件，前端专注展示和快捷入口。数据完整、请求少，风险集中在新增只读接口。
- 方案 C：直接把今日工作台做成看板首页的一部分。入口明显，但会重新增加首页噪音，违背第 1 步刚完成的减法方向。

**选择方案**

选择方案 B。今日工作台需要跨任务、日志、笔记和附件聚合，后端统一查询更可靠；第一阶段只做只读信息和跳转入口，不引入额外写入复杂度。

**本阶段扩展点**

- 日期范围支持“今天 / 本周 / 自定义”，后续可以继续扩展到“本月”或保存个人常用视图。
- 附件记录按来源区分任务、日志、笔记，后续可无缝升级为独立附件中心。
- 快捷入口先连接已有新建任务、写日志、写笔记、AI 页面，后续再加入模板和 AI 建议操作。

**实施内容**

- 新增只读接口 `GET /api/workbench?from=YYYY-MM-DD&to=YYYY-MM-DD`，聚合待办任务、进行中任务、即将截止任务、范围日志、最近笔记和范围附件。
- 前端新增 `api.getWorkbench`，由今日工作台一次性加载聚合数据。
- 顶部导航新增“今日”，默认进入今日工作台；看板、笔记、汇总、智能检索保持同级入口。
- 今日工作台支持“今天 / 本周 / 自定义”日期范围，并提供开始/结束日期输入与刷新。
- 今日工作台提供快捷入口：新建任务、写日志、新建笔记、打开 AI。
- “写日志”会优先打开进行中任务的任务抽屉，并直接定位到“日志”页签。
- 任务抽屉新增可选初始页签参数，支持从不同页面直达进度、日志、笔记或附件区域。
- 工作台页面展示四个轻量指标、三组任务重点、工作日志、最近笔记和附件记录。
- 附件记录按任务附件、日志附件、笔记附件展示来源，并保留下载入口。
- 新增桌面与移动端样式，手机宽度下工作台自动变为单列，日期控件和附件操作不挤压。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- `GET /api/workbench?from=2026-06-26&to=2026-06-26`：正常返回当前日期聚合数据。
- `GET /api/workbench?from=2026-06-23&to=2026-06-26`：正常返回任务、最近笔记和附件计数。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续执行第 3 步“笔记系统增强”。建议先实现笔记版本历史与 AI 整理回退，因为它能直接保护当前高频使用的 AI 整理功能，降低误覆盖风险。

## 第 3 步：笔记系统增强

### 第一阶段：笔记版本历史与 AI 整理回退

**计划比较**

- 方案 A：只在前端保存 AI 整理前草稿。实现快，但刷新或换浏览器后不可追溯，也无法覆盖普通编辑造成的误改。
- 方案 B：新增后端版本表，在每次正式更新笔记前后写入快照；AI 整理应用时标记来源为 `ai_format`，支持查看整理前后并回退。数据可靠，独立笔记和任务笔记都能复用。
- 方案 C：把完整审计日志系统扩展到所有任务、日志、笔记和附件。覆盖面最大，但会明显扩大本阶段复杂度，不适合先解决 AI 整理回退。

**选择方案**

选择方案 B。它能保护当前最常用、风险最高的笔记编辑与 AI 整理流程，又不会把第 6 步的数据审计和回收站提前混进来。

**本阶段扩展点**

- 版本表同时保存变更前和变更后快照，后续可以做更清晰的差异对比。
- 版本来源预留 `manual / ai_format / restore`，后续可继续扩展到模板套用、批量整理和 OpenClaw 操作。
- 回退当前只恢复笔记主体信息，后续可结合回收站与附件中心扩展到附件误删恢复。

**实施内容**

- 新增 `note_versions` 表，按笔记保存每次正式更新的变更前快照和变更后快照。
- `PATCH /api/notes/:id` 改为事务更新，更新成功后自动写入版本记录。
- 新增 `GET /api/notes/:id/versions`，用于读取单条笔记的版本历史。
- 新增 `POST /api/notes/:id/versions/:versionId/restore`，支持把笔记回退到某个版本的变更前内容，并把回退动作本身记录为新版本。
- AI 整理直接应用到笔记卡片时，版本来源标记为 `ai_format`，并保留整理想法作为版本备注。
- 编辑表单中先应用 AI 整理、再点击保存时，也会把该次保存标记为 `ai_format`。
- 笔记卡片新增“历史”按钮，打开版本历史抽屉。
- 版本历史抽屉支持查看版本列表、变更前预览、变更后预览，并可确认回退。
- 版本历史抽屉复用富文本预览能力，兼容旧纯文本笔记与现有富文本内容。
- 移动端版本历史抽屉自动变为单列布局，避免左右对比在窄屏挤压。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 后端启动检查：`node server/index.js` 可正常启动，后续已改为隐藏 API 进程运行。
- 版本接口闭环：创建临时笔记、AI 来源更新、读取版本、校验变更前/后快照、回退、确认生成回退版本，全部通过。
- 临时验证笔记清理：`GET /api/notes?search=version-smoke` 返回空数组。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 3 步第二阶段：笔记模板与 AI 整理预设。建议先做“模板/预设配置从代码内置开始，界面可选择套用”，再考虑用户自定义模板管理，避免一开始引入过多配置复杂度。

### 第二阶段：笔记模板与 AI 整理预设

**计划比较**

- 方案 A：新增数据库表和完整模板管理页面，支持用户创建、编辑、删除模板。长期灵活，但本阶段会引入较多配置与排序管理。
- 方案 B：先提供内置高频模板和内置 AI 整理预设，在创建笔记和 AI 整理抽屉中直接选择。实现轻、风险低，能马上覆盖账号记录、合同记录、客户沟通、问题排查、会议纪要等高频场景。
- 方案 C：只在 AI 提示词里告诉模型按模板整理。后端简单，但用户创建笔记时仍然没有结构化起点。

**选择方案**

选择方案 B。先把高频使用路径打通，不把模板管理复杂度提前引入；后续如果模板确实高频变化，再把内置模板升级为可配置模板。

**本阶段扩展点**

- 内置模板结构与表单字段保持一致，后续可迁移到数据库模板表。
- AI 整理预设只填充整理想法，不自动执行，继续保留用户确认。
- 预设覆盖清单整理、分组整理、提取待办、提取关键信息，后续可增加“合同审查”“账号资产表”等领域预设。

**实施内容**

- 新增内置笔记模板：账号记录、合同记录、客户沟通、问题排查、会议纪要。
- 新建笔记表单增加“常用模板”区域，点击模板后自动填入标题、分类和结构化正文。
- 模板只在新建笔记时展示，避免编辑已有笔记时误覆盖内容。
- 新增 AI 整理预设：按清单整理、按分组整理、提取待办、提取关键信息。
- AI 整理抽屉增加预设按钮，点击后填充“整理想法”，仍由用户手动开始整理与确认应用。
- 新增模板与预设按钮样式，保持为轻量工具条，不干扰正文编辑区域。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 3 步第三阶段：笔记与任务双向关联，以及富文本编辑器粘贴展示细节优化。建议先强化“笔记中查看/打开关联任务”和“任务中筛选相关笔记”，再处理更细的粘贴体验。

### 第三阶段：笔记任务双向关联与富文本粘贴展示优化

**计划比较**

- 方案 A：新增独立“全部笔记关系管理”页面。关系最清晰，但会增加一个新页面，和当前笔记页功能重叠。
- 方案 B：增强现有笔记页，支持“独立笔记 / 全部笔记”切换；笔记卡显示关联任务，并提供打开任务、取消关联、拖拽换关联。改动集中，能补齐双向关联。
- 方案 C：只在任务详情里展示任务笔记，不改独立笔记页。风险最低，但无法做到“笔记中查看关联任务”。

**选择方案**

选择方案 B。它复用当前笔记页和拖拽关联能力，能让任务与笔记互相可见、可跳转、可解除关系，同时避免新增复杂导航。

**本阶段扩展点**

- `GET /api/notes` 增加可选 `includeLinked` 查询，后续可扩展为全局笔记搜索。
- 笔记卡支持关联任务元信息，后续可继续加入任务状态、优先级和任务筛选。
- 富文本附件节点增加待上传标识和下载/打开提示，后续可继续扩展附件备注内联编辑。

**实施内容**

- `noteSelectSql` 增加关联任务标题、状态和优先级字段。
- 笔记返回结构增加 `taskTitle`、`taskStatus`、`taskPriority`，让前端不需要自行拼接任务信息。
- `GET /api/notes` 增加 `includeLinked=1` 查询参数，可在笔记页查询全部笔记。
- 修复仅修改笔记关联任务时 `content_json` fallback 写入 `[object Object]` 的问题，确保始终写入合法 JSON。
- 独立笔记页增加“独立笔记 / 全部笔记”范围切换。
- 笔记搜索扩展为可搜索标题、内容、分类或关联任务。
- 笔记卡新增关联任务信息条，显示关联任务标题和状态。
- 关联任务的笔记卡新增“打开任务”和“取消关联”操作。
- 笔记拖拽到任务仍可快速关联；在全部笔记视图中也可拖拽变更关联任务。
- 富文本附件节点增加“待保存 / 待保存上传”状态，粘贴或拖入文件后保存前更明确。
- 修复笔记页侧栏文案，将“拖动至任务关”改为“拖动至任务关联”。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 关联接口闭环：创建临时任务和笔记、关联任务、验证 `includeLinked=1` 返回关联任务信息、确认独立列表不显示已关联笔记、取消关联，全部通过。
- 临时验证数据清理：`GET /api/notes?search=note-link-smoke&includeLinked=1` 返回空数组；临时任务查询返回空数组。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**第 3 步收尾**

第 3 步“笔记系统增强”已完成当前方案内的主要内容：版本历史与 AI 回退、内置模板、AI 整理预设、任务双向关联和富文本粘贴展示优化。后续若继续深化，可把模板和预设迁移为用户可管理配置。

**下一阶段建议**

进入第 4 步“日志系统增强”。建议先做日志模板和“从笔记生成任务日志”，这两项能直接降低日常记录成本，并与第 3 步刚强化的笔记系统形成闭环。
## 第 4 步：日志系统增强

### 第一阶段：日志模板与从笔记生成日志草稿

**计划比较**

- 方案 A：新增数据库日志模板表和模板管理页，支持自定义新增、编辑、删除模板。长期最灵活，但会把“写日志”优化变成配置系统建设，当前阶段投入偏重。
- 方案 B：先提供内置高频日志模板，并支持从任务笔记生成日志草稿。改动集中在任务详情内，能立刻减少日常记录成本，且保留手动确认。
- 方案 C：从笔记一键直接创建正式日志。路径最短，但容易把未整理或不适合作为日志的笔记直接写入正式记录，误操作成本较高。

**选择方案**

选择方案 B。日志是工作进度的正式记录，应保留用户确认；内置模板能先覆盖跟进、沟通、文件、问题、阶段总结等高频场景，后续再根据实际使用频率升级为可管理模板。

**本阶段扩展点**

- 内置日志模板结构保留 `content / nextStep / hours / detailsOpen` 字段，后续可以迁移到数据库模板表。
- 从笔记生成日志采用草稿注入，不直接提交，后续可接入 AI 摘要，把长笔记压缩成更短的日志内容。
- 生成草稿会进入现有本机自动草稿机制，刷新页面后仍可恢复。

**实施内容**

- 新增内置日志模板：日常跟进、沟通记录、文件整理、问题处理、阶段总结。
- 任务详情的日志快捷录入区新增“常用模板”工具条，点击模板会填充工作内容、耗时和下一步计划。
- 任务笔记卡片新增“生成日志”入口，只在任务详情内的任务笔记显示。
- 点击“生成日志”后自动切换到日志页签，并把笔记标题、分类和正文生成一条待确认日志草稿。
- 从笔记生成的日志草稿不会直接提交，仍需用户确认后点击“记录日志”。
- 生成草稿会复用现有日志本机自动草稿机制，刷新后可恢复。
- 新增日志模板工具条样式，保持为轻量按钮，不挤压正文输入。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 4 步第二阶段：日志编辑历史与耗时范围筛选。建议先补后端 `log_versions` 表和 `GET /api/logs/:id/versions`，再把日志筛选扩展到最小/最大耗时，这样既能追溯修改，又能提高时间线检索效率。

### 第二阶段：日志编辑历史与耗时范围筛选

**计划比较**

- 方案 A：建立完整审计系统，覆盖任务、日志、笔记、附件所有修改，并支持统一检索与回退。覆盖面最大，但会提前进入第 6 步的数据安全与审计范围。
- 方案 B：只为工作日志新增版本快照表，记录每次编辑前后的字段，编辑抽屉内提供查看入口；同时扩展现有日志筛选，支持最小/最大耗时。改动聚焦，能直接解决日志追溯和检索效率。
- 方案 C：只在前端本机保存编辑前内容。实现快，但换浏览器或服务端部署后不可追溯，不能作为正式工作记录依据。

**选择方案**

选择方案 B。日志是任务进度的高频正式记录，版本历史应该保存在 MySQL；本阶段先做查看历史，不做回退，避免把日志编辑流程变重。

**本阶段扩展点**

- `log_versions` 采用 `before_snapshot / after_snapshot`，后续可以扩展恢复、差异高亮或统一审计页。
- 版本来源预留 `manual / restore / ai_format`，后续如果加入 AI 自动生成或日志回退，可以继续复用。
- 耗时筛选参数使用 `minHours / maxHours`，后续可继续扩展到“仅看有下一步计划”“仅看有附件”等筛选。

**实施内容**

- 新增 `log_versions` 表，按日志保存每次编辑前后的快照。
- 新增后端日志快照与版本映射逻辑，`PATCH /api/logs/:id` 保存成功后自动写入版本历史。
- 新增 `GET /api/logs/:id/versions`，返回单条日志的最近 80 条编辑历史。
- `GET /api/tasks/:id/logs` 增加 `minHours`、`maxHours` 查询参数，支持按耗时范围筛选。
- 前端 `api` 增加 `getLogVersions`。
- 任务详情日志筛选工具条新增“最少耗时 / 最多耗时”输入。
- 日志编辑抽屉新增“编辑历史”区，支持查看每次修改前后的日期、阶段、耗时、进度、内容和下一步计划。
- 移动端下日志历史的“修改前 / 修改后”自动变为单列展示，避免抽屉拥挤。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/db.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- API 闭环：创建临时任务与日志、编辑日志、确认 `log_versions` 产生 before/after 快照、使用 `minHours/maxHours` 命中和排除日志，全部通过，并已清理临时数据。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：清理旧后端进程后，新的后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 4 步第三阶段：从日志自动提取下一步计划，并建议创建任务。建议仍然采用“生成建议草稿，由用户确认创建”的方式，不让系统自动创建新任务。

### 第三阶段：从日志下一步生成任务草稿

**计划比较**

- 方案 A：调用 AI 从日志正文自动提取多个下一步，并批量建议任务。智能程度更高，但会引入模型成本、误提取和确认流程复杂度。
- 方案 B：优先使用日志已有的“下一步计划”字段生成一个新建任务草稿，打开现有任务弹窗由用户确认和修改。实现轻、可控，贴合当前日志结构。
- 方案 C：点击后直接创建任务。最快，但容易产生重复或不准确任务，不符合“建议创建”的确认思路。

**选择方案**

选择方案 B。当前日志已经有结构化的 `nextStep` 字段，先把它变成可编辑任务草稿最稳；AI 提取可以作为后续增强，在没有填写下一步时再辅助生成。

**本阶段扩展点**

- 新建任务弹窗支持 `initialValues`，后续可被 AI、OpenClaw 或模板继续复用。
- 日志转任务保留来源任务、日志日期、原日志内容和下一步计划到任务说明中，便于回溯。
- 后续可以增加“已由该日志创建任务”的关联标记，避免重复建议。

**实施内容**

- 新建任务弹窗新增 `initialValues` 支持，可带标题、说明、优先级、状态、进度、截止日期和标签打开。
- 主应用新增从日志生成任务草稿的入口逻辑：读取日志 `nextStep` 作为任务标题。
- 生成的任务说明会包含来源任务、来源日志日期、原日志内容和下一步计划。
- 任务详情日志时间线中，存在“下一步计划”的日志会显示“转为任务”按钮。
- 点击“转为任务”不会直接创建任务，而是打开新建任务弹窗，由用户确认后保存。
- 修复该按钮的 tiny 样式覆盖，避免文字按钮在来源/时间线区域被挤成竖排。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**第 4 步收口**

第 4 步“日志系统增强”已完成当前方案内的主要内容：日志模板、从任务笔记生成日志草稿、日志编辑历史、耗时范围筛选、从日志下一步生成任务草稿。后续如果继续深化，建议把“没有填写下一步计划的日志”接入 AI 提取建议，并增加“该日志已创建任务”的来源关联标记。

**下一阶段建议**

进入第 5 步“AI 助理增强”：建议先做任务 AI 快捷总结，复用现有任务范围对话，让单个任务的进展、日志、附件和下一步更容易被汇总。

## 第 5 步：AI 助理增强

### 第一阶段：任务 AI 快捷总结

**计划比较**

- 方案 A：新增专用任务总结接口，由后端聚合任务、日志、笔记和附件后再单独调用模型。结果可控，但会新增一条模型链路和一套保存/错误处理逻辑。
- 方案 B：复用现有任务范围 AI 连续对话，在任务详情 AI 面板增加快捷动作，一键发送“完整进展总结 / 提取下一步 / 任务复盘”等高频问题。实现轻、风险低，并继续保留流式输出与历史对话。
- 方案 C：完全由用户手动输入提示词。零开发，但高频使用时不够方便，也不符合“AI 助理增强”的目标。

**选择方案**

选择方案 B。当前 AI 面板已经支持任务范围、连续对话、流式输出、HTML 回答和历史记录；先补快捷动作能立刻提高任务复盘效率，不需要引入新的模型接口。

**本阶段扩展点**

- 快捷动作采用配置数组，后续可以继续加入“建议创建任务”“生成周报段落”“整理附件说明”等动作。
- 仍走现有 `/api/ai/ask-stream`，后续如果要做专用总结接口，可以复用同一套前端交互。
- 快捷动作只负责填充并发送问题，不直接执行创建/修改任务，保持用户确认原则。

**实施内容**

- 新增任务 AI 快捷动作配置：总结进展、提取下一步、任务复盘。
- `AiConversationShell` 抽出共用发送函数，输入框提交和快捷动作共用同一套流式输出、历史对话和来源展示逻辑。
- 任务详情 AI 面板在输入框上方显示快捷动作按钮；工作区 AI 页面保持原有简洁输入。
- 快捷动作使用现有任务范围 `/api/ai/ask-stream`，不会新增模型接口，也不会自动执行任务创建或修改。
- 新增快捷动作按钮样式，保持 OpenAI 风格的轻量圆角按钮，在手机宽度自动换行。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- AI 历史接口闭环：创建临时任务级对话、读取任务对话列表、读取消息列表、删除对话、清理临时任务，全部通过；该验证不触发模型调用。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 5 步第二阶段：AI 建议创建任务但必须用户确认。建议优先复用已有 MCP 操作审批/待确认请求机制，把 AI 的任务建议落为“待审批动作”，而不是直接写入任务表。

### 第二阶段：AI 建议创建任务并进入审批

**计划比较**

- 方案 A：只增加“建议建任务”快捷提问，让 AI 在聊天里输出建议。实现最快，但用户仍要手动复制到任务表单，不是真正的确认执行闭环。
- 方案 B：新增结构化任务建议接口，AI 返回任务建议列表；前端展示建议卡片，用户点击后把建议加入现有审批队列，最终仍由用户在“审批”里确认执行。闭环完整，且复用现有安全机制。
- 方案 C：AI 返回建议后直接创建任务。体验最快，但违背“必须由用户确认”的原则。

**选择方案**

选择方案 B。它让 AI 负责提出建议，让现有审批系统负责最终写入，既能降低手工复制成本，又不会绕过确认。

**本阶段扩展点**

- 结构化建议接口只面向任务详情，后续可扩展到工作区级别的“从全部笔记/日志发现待办”。
- 建议卡片生成的是 `create_task` 待审批动作，后续也可扩展为 `create_log`、`create_note` 或 `update_task` 建议。
- 模型建议与审批执行解耦，后续可以在审批卡片里加入差异预览、重复任务检测和来源引用。

**实施内容**

- 新增 `server/ai/task-suggestions.js`，根据单个任务的状态、最近日志、任务笔记和任务附件元信息生成结构化任务建议。
- 新增 `POST /api/ai/tasks/:id/suggestions`，返回 `{ taskId, suggestions }`。
- 新增 `POST /api/ai/task-suggestions/actions`，把某条建议转换为 `create_task` 待审批动作，来源标记为 `ai`。
- 前端 `api` 增加 `getTaskAiSuggestions` 和 `createAiTaskSuggestionAction`。
- 任务详情 AI 面板新增“AI 建议 / 后续任务”区域，支持生成建议、展示建议卡片、逐条加入审批。
- 加入审批后不直接创建任务；顶部“审批”按钮会收到待处理请求，仍由用户批准后执行。
- 新增建议卡片样式，移动端自动单列。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/ai/task-suggestions.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 审批闭环：通过 `POST /api/ai/task-suggestions/actions` 创建临时 AI 建议审批请求，确认出现在待审批列表，再拒绝清理，全部通过；该验证不触发模型调用。
- 文本扫描：核心文件、AI 建议模块与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：重启后端后，`3000` 健康接口正常，前端 `5173` 正常响应。

**下一阶段建议**

继续第 5 步第三阶段：AI 生成日报、周报、阶段总结和任务复盘。建议先在现有“汇总”页面增加“AI 生成总结”按钮，复用日期范围和报告数据。

### 第三阶段：汇总页 AI 生成日报/周报/阶段总结

**计划比较**

- 方案 A：在汇总页增加按钮，把当前报告内容填入 AI 对话输入框。实现轻，但用户仍需要跳转到 AI 页，且报告结果不会留在汇总页上下文中。
- 方案 B：抽出统一报告数据聚合函数，新增 AI 报告总结接口；汇总页按当前日期范围生成日报、周报或阶段总结，并直接展示安全 HTML。数据口径统一，体验完整。
- 方案 C：完全在前端根据现有报告数据拼接固定模板。稳定且无模型成本，但不是真正的 AI 总结，不能根据不同日志内容灵活提炼重点。

**选择方案**

选择方案 B。它复用现有报告数据，避免前后端统计口径不一致；AI 只负责表达、提炼和结构化展示，页面继续负责安全渲染与复制。

**本阶段扩展点**

- 报告数据聚合模块可继续供导出、备份和 AI 周报复用。
- AI 报告类型预留 `daily / weekly / stage`，后续可以增加“客户汇报”“内部复盘”等模板。
- HTML 渲染继续走现有 DOMPurify 白名单，后续可增加“导出 Markdown/PDF”。

**实施内容**

- 抽出 `server/report-data.js`，让普通汇总接口和 AI 总结接口复用同一套日报/周报数据口径。
- 新增 `server/ai/report-summary.js`，复用现有大模型网关配置生成可嵌入页面的安全 HTML 报告片段。
- `GET /api/reports` 改为调用统一报告聚合函数，避免后续导出、AI 总结和页面展示统计口径分裂。
- 新增 `POST /api/ai/reports/summary`，支持 `daily / weekly / stage` 三种报告类型。
- 前端汇总页增加 AI 总结面板，可按当前日期范围生成日报、周报或阶段总结。
- AI 总结结果在汇总页内直接展示，并支持复制纯文本内容。
- 汇总页报告模块标题统一为中文：完成内容、进行中任务、下一步计划。
- 新增 AI 报告面板样式，移动端自动换行，按钮和结果区不挤压。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/report-data.js`：通过。
- `node --check server/ai/report-summary.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- `GET /api/reports?from=2026-06-01&to=2026-06-26`：正常返回日志、进行中任务、已完成任务、下一步计划和耗时统计。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 说明：为避免不必要消耗模型额度，本阶段未直接调用 `POST /api/ai/reports/summary` 触发真实模型生成；接口语法、构建、页面接入和基础报告数据已完成验证。

**第 5 步收口**

第 5 步“AI 助理增强”已完成当前方案内的主要内容：任务 AI 快捷总结、AI 建议创建任务并进入审批、汇总页 AI 日报/周报/阶段总结入口，以及此前已完成的 AI 页面化、HTML 回答、来源卡和笔记 AI 整理确认。后续若继续深化，建议重点做检索质量评估和更多自动化审批类型。

**下一阶段建议**

进入第 6 步“数据安全与可靠性”。建议先做 `.env` 配置检查和附件存储健康检查，这两项不改变业务数据，却能提前发现部署和附件丢失风险。

## 第 6 步：数据安全与可靠性

### 第一阶段：配置检查与附件存储健康检查

**计划比较**

- 方案 A：先做完整备份、恢复、导出、回收站和权限体系。覆盖面最大，但会一次性改变大量数据路径，风险较高。
- 方案 B：先做 `.env`、数据库和附件存储健康检查，提供 API 与命令行脚本。只读为主，不改变业务数据，能先发现部署前风险。
- 方案 C：只写部署说明文档。风险最低，但不能真实检测当前环境和附件文件一致性。

**选择方案**

选择方案 B。当前第 6 步最需要先建立“看得见风险”的基础能力；配置检查和附件健康检查不影响任务、笔记、日志的正常使用，也能为后续备份、导出和服务器部署打底。

**本阶段扩展点**

- 健康检查模块可继续接入设置页或运维面板。
- 附件一致性检查已经覆盖日志附件、笔记附件和任务附件，后续可扩展为一键修复或孤立文件清理，但本阶段不自动删除。
- 系统检查脚本可作为服务器部署前的预检命令，后续登录和权限接入后仍可复用。

**实施内容**

- 新增 `server/reliability-checks.js`，统一输出运行配置、数据库和附件存储检查结果。
- 配置检查覆盖 `.env`、`.env.example`、`.gitignore`、MySQL、附件存储、AI、OCR、向量索引和登录模式。
- 数据库检查覆盖 MySQL 连接、当前数据库、时区和核心数据表。
- 附件存储检查覆盖 `log_attachments`、`note_attachments`、`task_attachments` 三类附件记录。
- 本地附件检查会校验文件是否存在、文件大小是否与数据库一致、路径是否越界，并扫描孤立文件样例。
- 新增 `GET /api/system/checks`，供前端或部署检查直接读取系统健康结果。
- 新增 `server/scripts/system-check.js` 和 `npm run check:system`，支持命令行运行；`--json` 可输出结构化结果。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/reliability-checks.js`：通过。
- `node --check server/scripts/system-check.js`：通过。
- `npm.cmd run check:system -- --json`：通过，返回 `warning`；唯一 warning 是当前 `AUTH_MODE=disabled`，符合“本机阶段暂不启用登录权限”的取舍。
- 附件健康检查：共 1 条附件记录，1 个本地文件已检查，缺失文件 0，大小不一致 0，异常路径 0，孤立文件 0。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 重启后端后，`GET /api/health` 正常返回。
- `GET /api/system/checks` 正常返回配置、数据库和附件健康结果。
- 前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 6 步第二阶段：数据备份与恢复。建议先做“备份生成与校验”脚本，把 MySQL 数据导出和 uploads 目录打包到本机 `backups` 目录；恢复能力先提供校验和说明，避免直接覆盖当前数据。

### 第二阶段：本机备份、校验与恢复预演

**计划比较**

- 方案 A：依赖 `mysqldump` 和系统压缩命令生成 SQL/ZIP 备份。格式通用，但依赖本机命令环境，Windows 上路径和编码更容易出问题。
- 方案 B：使用 Node 原生脚本导出 MySQL 表为 JSON，复制 `uploads`，生成 manifest 和 SHA256 校验；恢复默认只做预演，显式 `--apply --yes` 才执行覆盖。跨平台、可校验、误操作风险低。
- 方案 C：只复制 `uploads`，数据库仍靠手动导出。实现最轻，但不能完整恢复任务、日志、笔记和 AI 历史。

**选择方案**

选择方案 B。当前项目已经运行在 Node 环境里，用 Node 脚本能避免额外系统依赖；同时用 manifest 和校验值保证备份不是“看起来有文件，其实不可用”。

**本阶段扩展点**

- `backups` 目录已加入 `.gitignore`，避免把隐私数据和附件误提交。
- JSON 表备份后续可用于导出、迁移或调试数据问题。
- 恢复命令默认 dry-run，后续可在设置页接入“先校验、再确认恢复”的可视化流程。

**实施内容**

- 新增 `server/scripts/backup.js`，支持 `create / verify / restore` 三个命令。
- 新增 `npm run backup:create`，导出核心 MySQL 表到 `backups/<时间>/data/*.json`，并复制当前 `uploads` 文件。
- 新增 `manifest.json`，记录备份版本、创建时间、数据库名、表行数、附件数量、文件大小和 SHA256。
- 新增 `npm run backup:verify`，默认校验最近一次备份，也可指定备份目录。
- 新增 `npm run backup:restore`，默认只做恢复预演；真正恢复必须追加 `--apply --yes`。
- 真正恢复时会先校验备份，再恢复数据库；现有 `uploads` 会先重命名为安全副本，再复制备份内附件。
- `package.json` 增加备份相关脚本，`.gitignore` 增加 `backups/`。

**验证结果**

- `node --check server/scripts/backup.js`：通过。
- `npm.cmd run backup:create`：通过，生成 `D:\project\ssh\backups\20260626-131450`。
- 本次备份包含 15 张表、1 个上传文件和 16 个校验文件条目。
- `npm.cmd run backup:verify`：通过，16 个文件校验通过，无问题项。
- `npm.cmd run backup:restore`：通过，返回 `dry-run`，没有覆盖当前数据。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 6 步第三阶段：导出功能。建议先做 Markdown 和 Excel 导出，覆盖任务、日志、笔记和附件清单；PDF 可以后续基于稳定的 HTML/Markdown 导出再补。

### 第三阶段：汇总页 Markdown / Excel 导出

**计划比较**

- 方案 A：一次性实现 Markdown、Excel 和 PDF 三种导出。功能最完整，但 PDF 需要额外排版和渲染依赖，容易把本阶段变复杂。
- 方案 B：先实现 Markdown 和 Excel 导出，导出当前汇总页日期范围内的任务、日志、笔记和附件清单；PDF 等导出内容稳定后再补。收益明确、依赖少、验证快。
- 方案 C：只在前端复制当前页面文本。实现最快，但无法形成可归档文件，也不适合 Excel 统计。

**选择方案**

选择方案 B。Markdown 适合归档和发给别人，Excel 适合筛选统计；先把结构化导出跑通，再做 PDF 会更稳。

**本阶段扩展点**

- 后端导出数据聚合函数后续可复用给 PDF 或导出中心。
- Excel 工作簿按概览、任务、日志、笔记、附件分 sheet，后续可继续加筛选、公式和图表。
- Markdown 导出保留附件下载路径，后续可以改为完整外链或离线附件包。

**实施内容**

- 新增 `server/export-data.js`，聚合指定日期范围内的汇总数据、更新笔记和附件清单。
- 新增 `GET /api/exports/workspace?format=markdown|excel&from=YYYY-MM-DD&to=YYYY-MM-DD`。
- Markdown 导出包含概览、任务概览、工作日志、笔记和附件清单。
- Excel 导出包含 `概览 / 任务 / 日志 / 笔记 / 附件` 五个工作表，并设置表头、冻结首行和自动筛选。
- 汇总页工具条增加 `导出 Markdown` 和 `导出 Excel` 下载按钮，跟随当前日期范围。
- 增加导出按钮响应式样式，手机宽度下按钮自动换行。
- 修复下载接口中 `Content-Disposition` helper 作用域问题，确保导出路由正常返回文件。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/export-data.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 临时 `3001` 实例复测导出路由：Markdown 和 Excel 均返回 `200`。
- 重启当前后端后，正式 `3000` 导出接口验证通过：`export-smoke.md` 约 3.2 KB，`export-smoke.xlsx` 约 12 KB。
- Excel 文件读取验证：包含 `概览 / 任务 / 日志 / 笔记 / 附件` 五个工作表。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 6 步第四阶段：回收站。建议先对任务、笔记、日志三类核心数据做软删除和恢复；附件误删可以先跟随所属记录恢复，独立附件回收可放到附件中心阶段细化。

### 第四阶段：任务 / 日志 / 笔记回收站

**计划比较**

- 方案 A：对任务、日志、笔记、三类附件全部做软删除、恢复和彻底删除。保护范围最大，但附件涉及本地文件、OCR 缓存和富文本引用，容易把第 7 步附件中心提前复杂化。
- 方案 B：先对任务、日志、笔记三类核心业务数据做软删除和恢复；附件跟随所属记录保留，只有回收站里执行“彻底删除”时才清理数据库和本地文件。保护高频误删，风险可控。
- 方案 C：只在前端弹窗里提示确认，不改后端删除语义。实现最轻，但不能真正避免误删。

**选择方案**

选择方案 B。当前最容易误删的是任务、日志和笔记；先把这三类改成可恢复，附件独立回收放到后续“附件中心”阶段统一设计。

**本阶段扩展点**

- 三张核心表新增 `deleted_at / deleted_reason`，后续可以继续扩展删除人、保留天数、批量清空和自动清理。
- 回收站 API 使用通用 `type/id` 形式，后续可扩展到附件、AI 对话或审批记录。
- 恢复日志/笔记时会检查所属任务是否也在回收站，为后续更完整的层级恢复打基础。

**实施内容**

- `tasks`、`work_logs`、`task_notes` 新增软删除字段 `deleted_at` 和 `deleted_reason`。
- 普通删除任务、日志、笔记时不再硬删，改为写入删除时间和原因。
- 新增 `GET /api/trash`，返回回收站中的任务、日志和笔记。
- 新增 `POST /api/trash/:type/:id/restore`，支持恢复任务、日志和笔记。
- 新增 `DELETE /api/trash/:type/:id`，用于回收站内彻底删除；彻底删除时仍会清理关联附件文件、OCR 文本缓存和索引任务。
- 默认任务列表、今日工作台、汇总、导出、AI 检索、索引文档和 MCP 读取都排除回收站内容。
- 顶部导航新增“回收站”入口。
- 前端新增 `RecycleBinView`，支持按全部、任务、日志、笔记筛选，并提供恢复和彻底删除操作。
- 普通删除确认文案改为“移入回收站，可恢复”，把“无法撤销”只保留在彻底删除流程中。
- 增加回收站桌面与移动端样式，移动端卡片和按钮自动变为单列。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/db.js`：通过。
- `node --check server/validators.js`：通过。
- `node --check server/report-data.js`：通过。
- `node --check server/export-data.js`：通过。
- `node --check server/ai/search.js`：通过。
- `node --check server/ai/documents.js`：通过。
- `node --check server/mcp/read-tools.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 回收站 API 闭环：创建临时任务、日志、笔记；删除日志和笔记后确认进入回收站；恢复后确认回到任务时间线和笔记列表；删除任务后确认任务从普通列表消失并进入回收站；彻底删除任务后确认从回收站移除。
- 临时验证笔记清理：恢复后的 `trash-smoke-note` 已再次移入回收站并彻底删除，普通笔记搜索返回 0。
- 今日工作台接口验证：正常返回任务、笔记和附件聚合。
- 汇总接口验证：正常返回日志、未完成任务、已完成任务和总耗时。
- Markdown / Excel 导出接口验证：正常返回文件。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

第 6 步还剩 PDF 导出深化、附件独立回收和更完整的数据安全策略。考虑到第 7 步本身就是“附件中心”，建议先进入第 7 步第一阶段：独立附件中心，只做集中查看、筛选和反向跳转；附件独立回收与批量管理放在附件中心后续阶段继续推进。

## 第 7 步：附件中心

### 第一阶段：集中查看、筛选与来源跳转

**计划比较**

- 方案 A：一次性做完整附件中心，包括批量删除、附件独立回收、OCR 重试、预览墙和批量导出。功能最完整，但会把文件删除和恢复风险一起引入。
- 方案 B：先做只读聚合附件中心，集中查看任务附件、日志附件和笔记附件，支持筛选、搜索、预览/下载、OCR 状态展示和反向跳转。风险低，能马上提升查找文件效率。
- 方案 C：继续只在今日工作台和任务详情中分散显示附件。无需开发，但无法解决“按文件找来源”的问题。

**选择方案**

选择方案 B。附件中心第一阶段先作为文件资料索引页，不做危险写操作；批量管理和附件独立回收放到下一阶段更安全。

**本阶段扩展点**

- 附件中心接口按统一结构返回 `kind / attachment / source`，后续可直接加批量选择、附件回收和预览墙。
- 筛选参数预留 `kind / search / from / to / limit`，后续可继续扩展文件类型、OCR 状态和任务筛选。
- 前端卡片已显示 OCR 状态并复用重试能力，后续可以加“仅看识别失败”。

**实施内容**

- 新增 `GET /api/attachment-center`，聚合任务附件、日志附件和笔记附件。
- 附件中心接口支持按附件类型、关键词、上传日期范围和数量上限筛选。
- 聚合时默认排除回收站中的任务、日志和笔记来源。
- 返回结构包含附件下载/预览链接、OCR 状态、文件大小、备注、来源标题、任务 ID、日志 ID、笔记 ID 等信息。
- 前端 `api` 新增 `getAttachmentCenter`。
- 顶部导航新增“附件”入口。
- 新增 `AttachmentCenterView`，展示筛选工具条、附件类型切换和附件卡片网格。
- 附件卡片支持图片预览、文件下载、OCR 状态展示/重试，以及打开来源任务或切换到笔记页。
- 新增桌面和移动端样式，文件名截断、预览尺寸固定，窄屏下卡片和按钮不溢出。

**验证结果**

- `node --check server/index.js`：通过。
- `npm.cmd run build`：通过。
- `GET /api/attachment-center?kind=all&limit=20`：通过，当前返回 1 个笔记附件。
- `GET /api/attachment-center?kind=note&search=深圳&limit=20`：通过，搜索命中当前笔记附件。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 7 步第二阶段：附件中心筛选深化与预览墙。建议增加文件类型筛选、OCR 状态筛选和图片预览墙；附件独立回收可作为第三阶段，在确认中心页查找体验稳定后再做。

### 第二阶段：文件类型筛选、OCR 状态筛选与图片墙

**计划比较**

- 方案 A：直接做完整附件管理中心，加入批量选择、批量删除、附件回收和预览墙。管理能力完整，但删除类操作风险较高。
- 方案 B：先深化只读查找能力，增加文件类型筛选、OCR 状态筛选和图片墙视图；继续保留单个附件的既有下载、预览和 OCR 重试。体验提升明显，风险低。
- 方案 C：只在前端本地过滤当前列表。实现快，但数据量稍大时可能漏数据，也不利于后续加分页和统计。

**选择方案**

选择方案 B。附件中心当前最需要的是“更快找到文件”，不是立刻做危险的批量管理；筛选放在后端更可靠，也便于后续扩展分页。

**本阶段扩展点**

- 后端 `fileType` 支持 `image / pdf / document / spreadsheet / archive / other`，后续可继续扩展视频、音频或自定义类型。
- 后端 `textStatus` 支持 `none / pending / processing / completed / failed / unsupported`，后续可接入“仅看 OCR 失败”运维视图。
- 前端图片墙只使用当前筛选结果，后续可以继续扩展全屏预览、图片备注编辑和图片批量导出。

**实施内容**

- `GET /api/attachment-center` 增加 `fileType` 参数，按文件扩展名和 MIME 类型双重筛选。
- `GET /api/attachment-center` 增加 `textStatus` 参数，按附件 OCR/文本提取状态筛选。
- 附件中心返回 `fileType`、`textStatus` 和 `imageTotal`，便于前端展示当前筛选状态。
- 前端附件中心工具条新增“文件类型”选择：全部、图片、PDF、Word/文档、Excel/表格、压缩包、其他。
- 前端附件中心工具条新增“识别状态”选择：全部、已识别、待识别、识别中、识别失败、不支持、未入队。
- 前端新增列表/图片墙视图切换；图片墙展示当前筛选结果中的图片附件。
- 图片墙卡片支持打开预览、打开来源和下载。
- 增加图片墙与筛选工具条响应式样式，移动端下控件和图片卡片不溢出。

**验证结果**

- `node --check server/index.js`：通过。
- `npm.cmd run build`：通过。
- `GET /api/attachment-center?kind=all&fileType=all&textStatus=all&limit=20`：通过，当前返回 1 个附件。
- `GET /api/attachment-center?kind=all&fileType=pdf&limit=20`：通过，当前 PDF 附件命中。
- `GET /api/attachment-center?kind=all&fileType=image&limit=20`：通过，当前图片数为 0。
- `GET /api/attachment-center?kind=all&textStatus=completed&limit=20`：通过，当前已识别附件数为 0。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 7 步第三阶段：附件独立回收与批量管理。建议先做附件软删除和附件回收站，不直接批量物理删除；批量操作也必须走确认流程。

### 第三阶段：附件独立回收与批量管理

**计划比较**

- 方案 A：附件中心直接支持批量彻底删除。操作最短，但误删文件后无法恢复，风险过高。
- 方案 B：先为三类附件增加软删除字段，所有普通删除都进入附件回收站；附件中心只做批量移入回收站，彻底删除只在回收站单项执行。可恢复、可验证，风险更可控。
- 方案 C：继续沿用详情页单个附件删除。实现成本低，但附件中心无法承担集中管理职责，且删除后缺少恢复路径。

**选择方案**

选择方案 B。附件属于本地文件资料，误删成本高；先做软删除和回收站闭环，再考虑更激进的批量物理删除。

**本阶段扩展点**

- `log_attachments / note_attachments / task_attachments` 均新增 `deleted_at / deleted_reason`，后续可以继续加删除人、保留期限和自动清理策略。
- 附件中心批量接口只接受 `{ kind, id }` 项，后续可扩展批量打标签、批量导出和批量 OCR 重试。
- 回收站新增 `attachment` 类型，后续可以做附件批量恢复、按来源筛选和过期清理。

**实施内容**

- 数据库迁移为日志附件、笔记附件和任务附件增加 `deleted_at`、`deleted_reason`。
- 附件映射结构增加 `deletedAt`、`deletedReason`。
- 所有普通附件列表、AI 检索、文档索引、导出、MCP 读取和附件中心默认过滤已删除附件。
- 三个原有附件删除接口改为软删除：日志附件、任务附件、笔记附件都会进入附件回收站，不再立即删除本地文件。
- 笔记附件软删除时仍会从笔记正文富文本节点中移除，恢复后保留在附件列表，不自动重新插回正文。
- 新增 `PATCH /api/attachment-center/trash`，支持附件中心批量移入回收站。
- `GET /api/trash` 支持 `type=attachment`，并在 `type=all` 时返回附件回收项。
- 新增 `POST /api/trash/attachment/:kind/:id/restore`，恢复前检查所属任务、日志或笔记是否也在回收站。
- 新增 `DELETE /api/trash/attachment/:kind/:id`，仅对已在附件回收站的文件执行彻底删除，并清理文本缓存和本地文件。
- 附件中心新增多选、当前页全选、清空选择和“移入回收站”批量操作条。
- 附件中心列表卡片和图片墙卡片增加选择框与选中态。
- 回收站新增“附件”分类，附件项显示文件大小、MIME 类型、所属任务、所属日志或所属笔记，并复用恢复/彻底删除操作。
- 回收站任务卡的附件数量改为只统计仍然有效的任务附件，避免把已进附件回收站的文件算进去。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check server/db.js`：通过。
- `node --check server/validators.js`：通过。
- `node --check server/export-data.js`：通过。
- `node --check server/ai/search.js`：通过。
- `node --check server/ai/documents.js`：通过。
- `node --check server/mcp/read-tools.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- 附件回收 API 闭环：创建临时任务并上传 CSV 附件；确认附件中心可见；批量移入回收站后附件中心不再显示；回收站附件列表可见；恢复后附件中心重新显示；再次移入回收站后彻底删除；最后清理临时任务。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 7 步第四阶段：附件预览与资料整理能力。建议优先做图片全屏预览、附件备注快速编辑、按任务/笔记来源筛选，以及“识别失败附件”集中重试；批量永久删除继续暂缓。

### 第四阶段：附件预览与资料整理能力

**计划比较**

- 方案 A：一次性做完整文件管理器，包括目录、批量导出、批量预览和文件重命名。能力强，但会偏离当前任务台的高频需求。
- 方案 B：围绕附件中心继续补高频动作：按任务筛选、图片全屏预览、附件备注快速编辑、选中附件重新识别。实现可控，也能明显提升查找和整理效率。
- 方案 C：只保留当前下载/跳转能力。稳定但使用时仍需要频繁切换任务详情，附件中心的整理价值不足。

**选择方案**

选择方案 B。本阶段继续把附件中心做成“资料查找和整理入口”，不扩展成复杂网盘。

**本阶段扩展点**

- `GET /api/attachment-center` 增加 `taskId`，后续可继续扩展 `noteId / logId / failedOnly` 等来源筛选。
- 新增统一附件备注更新接口，后续可继续扩展附件标签、重要标记和分类。
- 前端图片预览以独立覆盖层实现，后续可继续做上一张/下一张和键盘关闭。

**实施内容**

- `GET /api/attachment-center` 新增 `taskId` 参数，任务附件、日志附件和任务笔记附件都可按所属任务过滤。
- 新增 `PATCH /api/attachment-center/:kind/:id`，统一更新任务附件、日志附件和笔记附件备注。
- 前端 `api` 新增 `updateCenterAttachment`。
- 附件中心工具条新增“任务”下拉筛选，支持只看某个任务相关附件。
- 附件中心增加图片全屏预览覆盖层，展示图片、文件名、来源和下载入口。
- 附件中心卡片的图片预览从新标签跳转改为站内全屏预览；非图片仍保留原有预览/下载行为。
- 附件卡片新增备注快速编辑：默认展示备注或“暂无备注”，点击编辑后可直接保存。
- 批量操作条新增“重试识别”，对选中且非识别中的附件批量提交 OCR/文本提取重试。
- 补充全屏预览、备注编辑区和移动端批量条样式。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- 附件中心第四阶段接口验证：创建临时任务并上传 CSV 附件；`taskId` 筛选命中该附件；通过统一备注接口更新备注；按新备注搜索可命中；临时任务和附件已清理。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

第 7 步附件中心的主要目标已完成。建议回到第 6 步补“PDF 导出深化”，或者进入第 4 步剩余增强项“日志模板与从笔记生成日志”。优先级上，我建议先做日志模板，因为它能直接降低每天记录工作的成本。

## 第 6 步回补：PDF 导出深化

### PDF 导出

**计划比较**

- 方案 A：用 Python `reportlab` 生成 PDF。排版可靠，但会让 Node 服务部署时额外依赖 Python 运行环境。
- 方案 B：用 Node 侧 `pdfkit` 生成 PDF，并通过可配置中文字体保证中文显示。部署路径更贴合当前 Express 服务。
- 方案 C：只导出 Markdown/Excel，让用户自行转 PDF。实现零成本，但不满足优化方案中的 PDF 导出目标。

**选择方案**

选择方案 B。当前项目是 Node/Express 服务，PDF 生成留在 Node 进程内更方便部署；中文字体通过 `PDF_FONT_PATH` 和常见系统字体候选兜底。

**本阶段扩展点**

- `.env.example` 增加 `PDF_FONT_PATH`，服务器部署时可明确指定中文字体。
- 系统检查新增“PDF 中文字体”项，部署前能快速发现中文字体缺失。
- PDF 导出采用“摘要 + 分区卡片式列表”，后续可继续扩展封面、页眉页脚和更精细的表格排版。

**实施内容**

- 新增依赖 `pdfkit`。
- `server/export-data.js` 新增 `createPdfExport(data)`，生成包含任务概览、工作日志、笔记和附件清单的 PDF。
- PDF 导出支持中文字体候选：优先 `PDF_FONT_PATH`，本机可自动使用 `C:/Windows/Fonts/NotoSansSC-VF.ttf`。
- `server/index.js` 的 `/api/exports/workspace` 支持 `format=pdf`，返回 `application/pdf`。
- 汇总页导出区新增“导出 PDF”按钮。
- `.env.example` 增加 `PDF_FONT_PATH` 配置说明。
- `server/reliability-checks.js` 增加 PDF 中文字体检查。

**验证结果**

- `node --check server/export-data.js`：通过。
- `node --check server/index.js`：通过。
- `node --check server/reliability-checks.js`：通过。
- `npm.cmd run build`：通过。
- `GET /api/exports/workspace?format=pdf`：返回有效 PDF 文件头 `%PDF-`，大小约 48 KB。
- `HEAD /api/exports/workspace?format=pdf`：返回 `application/pdf`。
- 使用 `pdf-parse` 解析临时 PDF：可读出 4 页中文内容，包含导出标题、日期范围、任务概览、笔记和附件等正文。
- 本地 Poppler 包装器存在但缺少实际 exe，未完成 PNG 渲染；已用 PDF 文件头、Content-Type 和文本解析作为兜底验证。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，PDF 中文字体为 OK，仍只有登录未启用的预期 warning。
- 服务检查：后端 `3000` 健康接口正常，前端 `5173` 正常响应。
- 文本扫描：核心文件、`.env.example` 与两个 Markdown 文档未发现目标英文占位、问号占位或典型 mojibake 字符。

**下一阶段建议**

继续第 4 步剩余增强项：日志模板与从笔记生成日志。这个功能能直接减少每天写日志的输入成本，也能和现有 AI 整理、笔记系统形成更顺的工作流。

## 第 4 步回补：日志模板与从笔记生成日志

### 日志模板与笔记转日志草稿

**计划比较**

- 方案 A：新增数据库日志模板表，支持全局模板的增删改查。结构完整，但当前是个人本机使用，模板同步和权限暂时不是核心问题。
- 方案 B：基于现有日志录入区做本地自定义模板，模板保存在浏览器本机；同时增强“从笔记生成日志草稿”的默认字段。实现轻、风险低，能马上减少日常输入成本。
- 方案 C：只保留已有固定模板和笔记生成按钮。无需新增实现，但模板不能贴合个人工作习惯，笔记转日志仍要补较多字段。

**选择方案**

选择方案 B。日志模板属于高频个人习惯配置，先放在本机浏览器草稿层最合适；等以后部署多端或多人使用时，再升级为数据库模板。

**本阶段扩展点**

- 自定义日志模板使用独立 localStorage key，后续可平滑迁移到后端 `log_templates` 表。
- 模板结构保留 `content / nextStep / hours / detailsOpen`，后续可扩展默认阶段、附件备注和标签。
- 从笔记生成日志使用现有 `LogComposer seed` 机制，后续可接 AI 提取“工作内容/下一步/耗时建议”。

**实施内容**

- 新增自定义日志模板本地存储：`assistant-task-board:custom-log-templates:v1`。
- 日志录入区模板列表现在同时显示默认模板和自定义模板。
- 支持把当前日志内容保存为模板，保存内容包括工作内容、耗时、下一步计划和是否展开详情。
- 自定义模板支持删除，默认模板保持不可删除。
- 从任务笔记生成日志草稿时，自动带入 0.25 小时、当前阶段、当前进度快照和下一步计划，并自动展开补充详情。
- 清理日志录入区旧英文占位：日志搜索、工作内容、下一步计划、附件备注等全部改为中文。
- 修复日志编辑抽屉中“下一步计划”输入框缺失的问题。
- 修正附件删除确认文案：普通删除进入回收站，可恢复，不再提示会直接删除本地文件。
- 增加自定义模板按钮、删除按钮、保存模板输入行的桌面和移动端样式。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、问号占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续第 4 步剩余增强项：从日志自动提取下一步计划，并建议创建任务。当前日志时间线已经有“下一步转为任务”的手动入口，下一阶段可增加更清晰的建议卡和 AI 提取能力。

### 日志下一步自动提取与任务建议

**计划比较**

- 方案 A：每次写日志都调用 AI 提取下一步计划。语义能力强，但会增加延迟和模型成本，也容易在高频输入时打扰。
- 方案 B：先做本地轻量规则提取，识别“下一步 / 待办 / TODO / 后续 / 计划 / 需要跟进”等明确句式，用户确认后填入下一步计划；创建任务仍复用现有时间线按钮。成本低、反馈快、可控。
- 方案 C：只保留时间线里的“下一步转为任务”按钮。稳定但用户仍需要手动把下一步从正文复制到专门字段。

**选择方案**

选择方案 B。日志录入是高频操作，轻量规则足够覆盖常见写法，也不会让 AI 误改正式记录；后续再把 AI 提取作为增强入口。

**本阶段扩展点**

- `inferNextStepFromLogContent` 是纯前端函数，后续可以替换或叠加 AI 提取。
- 建议卡只在 `nextStep` 为空时出现，避免干扰已经手动填写的日志。
- 建议填入后仍走现有 `nextStep` 字段和“转为任务”流程，不新增数据模型。

**实施内容**

- 新增 `inferNextStepFromLogContent(content)`，按行识别下一步计划类句式。
- 日志录入区在工作内容下方新增“识别到可能的下一步”建议卡。
- 点击“填入下一步计划”后，自动写入 `nextStep`，展开补充详情，并提示保存后可在时间线转为任务。
- 不自动创建任务，仍保持用户确认后通过时间线按钮转换。
- 新增建议卡样式，保持轻量提示，不打断快捷录入。

**验证结果**

- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与执行记录未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续第 4 步剩余增强项：从笔记一键生成任务日志的 AI 增强版。当前已支持规则式笔记转日志草稿，下一阶段可让 AI 根据单条笔记提取“完成内容、耗时建议、下一步计划”，同样由用户确认后写入日志草稿。

### AI 根据单条笔记生成日志草稿

**计划比较**

- 方案 A：AI 直接根据笔记创建正式日志。最省点击，但会让模型输出直接写入工作记录，风险过高。
- 方案 B：AI 只生成日志草稿字段，填入现有日志录入区，由用户确认后点击“记录日志”。既能减少整理成本，也保留人工确认。
- 方案 C：只使用前端规则式笔记转日志。稳定但对杂乱笔记的提炼能力有限，不能很好提取耗时和下一步计划。

**选择方案**

选择方案 B。日志属于正式工作记录，AI 只能辅助生成草稿，最终写入仍由用户确认。

**本阶段扩展点**

- 新增 `server/ai/log-draft.js` 独立模块，后续可加入流式草稿、用户整理想法或更多上下文。
- 新增接口只读取当前任务和所属笔记，不信任前端传来的正文，避免跨任务误用。
- 前端复用现有 `LogComposer seed` 机制，不新增日志保存路径。

**实施内容**

- 新增 `generateLogDraftFromNote({ task, note })`，调用 LiteLLM/OpenAI 兼容接口返回 JSON 草稿。
- AI 草稿字段包括：`content`、`hours`、`nextStep`、`stage`、`progressSnapshot`。
- 后端对 AI 返回做归一化：耗时限制到常用值，阶段必须是合法状态，进度限制在 0-100，正文和下一步限制长度。
- 新增 `POST /api/ai/tasks/:taskId/notes/:noteId/log-draft`，校验任务存在、笔记存在且属于当前任务。
- 前端 `api` 新增 `generateLogDraftFromNote`。
- 任务笔记卡片新增 `AI生成日志` 按钮，生成期间显示加载态。
- AI 生成成功后填入现有日志录入区，并提示“请确认后再记录”；不会自动保存正式日志。
- 规则式 `生成日志` 保留，适合不想调用 AI 时快速使用。

**验证结果**

- `node --check server/ai/log-draft.js`：通过。
- `node --check server/index.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- mock 模型响应测试：`generateLogDraftFromNote` 可正确解析并归一化草稿字段。
- 服务重启后，新接口非法任务请求返回 404，说明路由已挂载且任务校验生效。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与执行记录未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

第 4 步日志增强的主要项目已基本完成。建议回到第 5 步 AI 助理增强，继续补“AI 总结单个任务完整进展”，把任务状态、关键日志、笔记和附件整理成任务复盘卡片。

### AI 总结单个任务完整进展

**计划比较**

- 方案 A：把任务进展总结做成普通聊天预设问题。实现最轻，但每次都依赖用户自己提问，结果也不一定稳定展示成复盘结构。
- 方案 B：新增专用任务总结接口，后端读取任务、最近日志、任务笔记和附件元信息，AI 只输出安全 HTML 片段，前端在任务详情智能页签中以复盘卡片展示。
- 方案 C：完全本地规则生成总结，不调用 AI。稳定且无成本，但对杂乱日志、笔记和附件要点的组织能力有限。

**选择方案**

选择方案 B。单个任务总结属于高价值低频操作，适合调用 AI；同时用专用接口约束上下文和输出结构，避免普通聊天回答散乱。

**本阶段扩展点**

- 新增 `server/ai/task-summary.js` 独立模块，后续可扩展为阶段复盘、任务结案报告或导出 PDF。
- 任务上下文复用现有 `getTask(taskId)` 聚合层，不新增数据库查询分支。
- 前端复用现有 `toSafeAiHtml` 清洗逻辑和 AI HTML 样式，后续所有 AI 数据面板可以统一优化。

**实施内容**

- 新增 `summarizeTaskWithAi(taskId)`，读取任务状态、进度、最近日志、任务笔记、任务附件和数量统计。
- AI prompt 约束为“只根据提供资料总结”，输出可嵌入页面的安全 HTML 片段，不允许脚本、样式、表单或事件属性。
- prompt 建议结构包含：任务总览数据面板、当前进展、关键日志、笔记要点、附件情况、风险/阻塞、下一步建议。
- 新增 `POST /api/ai/tasks/:id/summary`，任务不存在时返回 404，模型不可用时返回明确错误。
- 前端 `api` 新增 `getTaskAiSummary(taskId)`。
- 任务详情“智能”区域顶部新增 `AI 复盘 / 任务进展总结` 面板，支持生成和重新生成。
- 总结结果使用现有安全 HTML 渲染规则展示，进度条、指标卡、任务卡等样式继续复用当前 AI 回答样式。

**验证结果**

- `node --check server/ai/task-summary.js`：通过。
- `node --check server/index.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- mock 模型响应测试：临时创建任务、日志和笔记后，`summarizeTaskWithAi` 能读取聚合上下文并返回 HTML 总结和正确计数；测试结束后已删除临时任务。
- 服务重启后，新接口非法任务请求返回 404，说明路由已挂载且任务校验生效。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续第 5 步 AI 助理增强：把 AI 总结能力延伸到“按日期范围生成 AI 周报/阶段复盘的确认稿”，并逐步补齐 OpenClaw 可执行操作前的审批与审计记录。

### AI 汇总确认后保存为笔记

**计划比较**

- 方案 A：AI 生成日报、周报或阶段总结后只支持复制。实现最轻，但内容容易散落到外部文档，后续无法在任务台内检索和整理。
- 方案 B：在现有 AI 汇总结果区增加“保存为笔记”，由用户确认后创建独立笔记。实现范围小，能把总结沉淀进现有笔记系统。
- 方案 C：每次 AI 生成后自动保存为笔记。省一步点击，但会产生大量不一定满意的笔记，且不符合“AI 结果由用户确认”的原则。

**选择方案**

选择方案 B。AI 汇总属于确认稿，保存动作应该由用户决定；保存为独立笔记后可以继续搜索、AI 整理、分类和归档。

**本阶段扩展点**

- 保存逻辑复用 `createStandaloneNote`，不新增数据表和后端接口。
- AI HTML 先转换成干净纯文本，再写入笔记富文本结构，避免把不受信任的 HTML 直接保存为笔记正文。
- 保存后刷新独立笔记列表和分类统计，后续可继续扩展为“保存到指定任务笔记”或“保存到指定分类”。

**实施内容**

- 汇总页 `ReportView` 新增 `addToast` 和 `onNoteSaved` 回调。
- AI 汇总结果区新增操作组：`复制总结` 与 `保存为笔记`。
- 保存时自动生成标题，例如 `周报：2026-06-22 至 2026-06-26`。
- 保存时自动使用分类 `AI汇总`，正文包含汇总类型、时间范围和 AI 总结纯文本。
- 保存成功后按钮变为 `已保存为笔记`，避免重复保存同一份结果。
- 重新生成 AI 汇总或切换日期范围时，保存状态会重置。
- 保存成功后刷新独立笔记列表与笔记分类统计。
- 新增 `.ai-report-result-actions` 样式，复制和保存按钮在桌面和移动端都保持横向可换行的操作组。

**验证结果**

- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 服务检查：前端 `5173` 正常响应。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续第 5 步 AI 助理增强：优化 AI 回答中的任务、链接、附件和文件来源操作卡，让“打开任务、复制链接、下载文件、保存为笔记”等动作更一致。

### AI 来源卡与可操作内容优化

**计划比较**

- 方案 A：只调整来源区 CSS。能快速缓解按钮挤压，但来源为什么出现、点击后跳到哪里仍不清楚。
- 方案 B：同时优化来源卡结构、操作按钮组和来源跳转规则。范围仍主要在前端，但能明显提升 AI 回答后的使用闭环。
- 方案 C：重新设计完整 AI 结果组件和后端来源结构。潜力大，但本阶段改动过重，容易影响已有对话历史和流式输出。

**选择方案**

选择方案 B。当前后端已经返回 `entityType / reason / links / downloadUrl / previewUrl` 等结构化字段，前端可以先把这些信息用好。

**本阶段扩展点**

- 新增 `sourceTypeLabel(source)`，后续可继续扩展为不同来源颜色、图标和排序。
- 来源点击规则按类型打开任务详情页签，后续可进一步定位到具体日志、笔记或附件。
- 链接和附件动作统一放入 `.ai-action-card-actions`，后续新增“保存到笔记”“创建任务”等按钮不会破坏布局。

**实施内容**

- AI 来源点击跳转更精准：
  - 日志来源打开任务详情的 `日志` 页签。
  - 任务笔记来源打开 `笔记` 页签。
  - 任务附件来源打开 `附件` 页签。
  - 独立笔记和独立笔记附件跳转到 `笔记` 页面。
- 来源卡新增来源类型标签，例如 `任务`、`日志`、`任务附件`、`独立笔记附件`。
- 来源卡展示后端返回的匹配原因，例如 `任务匹配`、`任务日志匹配`、`笔记强匹配`。
- 链接操作卡的 `打开 / 复制链接` 改为统一动作组。
- 文件操作卡的 `预览 / 下载 / 复制文件名` 改为统一动作组，避免按钮数量多时把文字挤成竖排。
- 移动端下动作组自动换到内容下方并左对齐，减少窄屏拥挤。

**验证结果**

- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 服务检查：前端 `5173` 正常响应。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续第 5 步 AI 助理增强：把“AI 建议创建任务”的审批链路展示得更清楚，补充审批结果反馈、来源信息和可审计记录。

### AI 建议创建任务审批体验补强

**计划比较**

- 方案 A：只保留现有审批队列和原始 JSON。功能完整，但日常审批时需要读技术字段，确认成本高。
- 方案 B：保留原始 JSON，同时新增人类可读的字段摘要、来源说明和更明确的中文反馈。改动小，能提升确认体验。
- 方案 C：重做完整审批中心，加入历史、筛选、批量审批和审计详情。更完整，但超出当前 AI 助理增强的局部目标。

**选择方案**

选择方案 B。审批的核心是“用户确认后执行”，因此第一眼必须能看懂这个动作会创建什么、从哪里来、为什么建议。

**本阶段扩展点**

- AI 后续任务建议进入审批时，payload 现在包含 `sourceTaskId` 和 `sourceReason`。
- 审批卡新增 `ActionPayloadSummary`，后续可按 `update_task / create_log / create_note` 等动作继续扩展专属摘要。
- 原始 JSON 仍保留在折叠区，方便排查和审计。

**实施内容**

- `POST /api/ai/task-suggestions/actions` 在 action payload 中记录来源任务 ID 和来源说明。
- 任务详情里的 `AI 建议 / 后续任务` 点击“加入审批”时，自动带入当前任务 ID 和来源说明。
- 审批弹窗标题说明改为“AI 或外部智能体”，覆盖当前 AI 建议和未来 OpenClaw 写入动作。
- 审批工具条英文 `Refreshing / Pending` 改为中文 `正在刷新 / 待审批 N 条`。
- 审批卡新增字段摘要：
  - 任务标题
  - 优先级
  - 状态
  - 截止日期
  - 标签
  - 来源任务
  - 来源说明
  - 任务说明
- 折叠按钮改为 `查看 JSON / 收起 JSON`，保留原始 payload。
- 拒绝动作反馈改为 `已拒绝`，不再显示笼统的“提示 / 操作已完成”。
- 新增 `.approval-summary` 样式，桌面两列、手机单列。

**验证结果**

- `node --check server/index.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 服务重启后，临时 AI 任务建议审批请求返回的 payload 已包含 `sourceTaskId` 和 `sourceReason`；测试请求已清理。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

第 5 步 AI 助理增强已覆盖主要可用项。建议转入第 6 步数据安全与可靠性，继续补“本机备份与恢复”能力；登录和权限保持在服务器部署前再做。

## 第 6 步回补：备份可视化入口

### 系统页备份创建与校验

**计划比较**

- 方案 A：重新实现一套网站内备份与恢复系统，包括恢复上传、覆盖数据库和附件。功能完整，但恢复是高风险动作，不适合在没有权限与二次确认前放到页面里。
- 方案 B：复用现有 `server/scripts/backup.js`，把备份创建、备份列表和备份校验暴露为低风险 API；前端新增“系统”页，只提供创建与校验，不提供页面恢复。
- 方案 C：继续只保留命令行备份脚本。稳定但不够顺手，用户很难知道最近是否有备份。

**选择方案**

选择方案 B。现有备份脚本已经完成 MySQL JSON、uploads 复制、manifest 和 SHA256 校验；本阶段只把它接入网站可视化入口，不把危险恢复放进页面。

**本阶段扩展点**

- `backup.js` 已模块化导出 `createBackup / verifyBackup / listBackups / latestBackupDir`，命令行和 API 共用同一套实现。
- 恢复仍保留在命令行 `npm run backup:restore` 的 dry-run / `--apply --yes` 双确认流程中。
- 系统页后续可以继续接入系统检查、附件健康检查、备份下载或备份清理策略。

**实施内容**

- `server/scripts/backup.js` 改为可导入模块，同时保留原命令行行为。
- 备份脚本新增 `listBackups({ limit })`，读取 `backups` 目录下的 manifest 摘要。
- 新增 `GET /api/backups`，返回最近备份列表。
- 新增 `POST /api/backups`，创建本机备份。
- 新增 `POST /api/backups/verify`，校验指定备份；未传目录时校验最近备份。
- 前端 `api` 新增 `getBackups / createBackup / verifyBackup`。
- 顶部导航新增 `系统` 入口。
- 新增 `SystemView`：
  - 自动展示备份列表。
  - 支持创建备份。
  - 支持校验最近备份。
  - 展示数据表数量、总行数、附件数量、附件大小、校验文件数量和备份目录。
  - 明确提示恢复仍需命令行预演确认。
- 新增系统页和备份卡片样式，桌面多列、移动端单列。

**验证结果**

- `node --check server/scripts/backup.js`：通过。
- `node --check server/index.js`：通过。
- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- 服务重启后：
  - `GET /api/backups?limit=3`：正常返回备份列表。
  - `POST /api/backups`：通过，生成 `D:\project\ssh\backups\20260626-152237`。
  - `POST /api/backups/verify`：通过，校验 16 个文件，无问题。
- `npm.cmd run backup:verify`：通过，说明命令行脚本仍可用。
- `npm.cmd run smoke`：通过。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 服务检查：前端 `5173` 正常响应。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

回到第 3 步笔记系统增强，继续补“笔记模板和 AI 整理预设”的更细体验；或者进入部署准备文档，但登录和权限仍按要求留到服务器部署前再实现。

## 第 3 步回补：笔记模板体验

### 自定义笔记模板

**计划比较**

- 方案 A：把笔记模板做成数据库表，支持全局管理、排序和多端同步。完整但会新增管理页面和数据迁移，当前个人本机使用略重。
- 方案 B：先用浏览器本机存储保存自定义笔记模板，复用现有模板面板；支持保存当前标题、分类和正文为模板，支持删除自定义模板。
- 方案 C：只保留内置模板。稳定但不能覆盖你的个人格式习惯。

**选择方案**

选择方案 B。笔记模板属于个人输入习惯，先本地保存最轻；等后续部署多端使用时再迁移到数据库模板表。

**本阶段扩展点**

- 自定义模板使用独立 localStorage key：`assistant-task-board:custom-note-templates:v1`。
- 模板结构保留 `label / title / category / content`，后续可迁移到后端并扩展排序、分类和共享。
- 不自动创建分类；分类仍由用户输入并在保存笔记后自然进入分类候选。

**实施内容**

- 新增自定义笔记模板归一化、读取和保存函数。
- 创建笔记表单的模板面板现在同时显示内置模板和自定义模板。
- 支持把当前笔记标题、分类和正文保存为自定义模板。
- 自定义模板支持删除，内置模板保持不可删除。
- 保存模板不会提交正式笔记，也不会自动创建分类。
- 移动端下保存模板输入行自动单列展示。
- AI 整理预设按钮样式保持原样，避免本次模板样式影响已有 AI 整理体验。

**验证结果**

- `node --check src/api.js`：通过。
- `npm.cmd run build`：通过。
- `npm.cmd run smoke`：通过。
- 服务检查：前端 `5173` 正常响应。
- `npm.cmd run check:system -- --json`：通过，仍只有登录未启用的预期 warning。
- 文本扫描：核心文件与两个 Markdown 文档未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

继续做部署准备文档和运维说明，把 `.env`、MySQL、备份、OCR/AI 模型、附件目录、反向代理和服务器部署前登录权限补充成一份可执行清单。

## 部署准备文档

### 服务器部署准备清单

**计划比较**

- 方案 A：直接开始实现登录、权限和生产部署脚本。能推进服务器化，但用户已明确当前阶段不需要登录权限实现。
- 方案 B：新增部署准备文档，把必须启用登录权限、HTTPS、备份、AI/OCR、OpenClaw、附件存储等前置条件写清楚，不改运行代码。
- 方案 C：只依赖 `.env.example` 注释。文件短，但缺少部署步骤、检查命令和恢复注意事项。

**选择方案**

选择方案 B。它满足“服务器部署前加登录和权限，目前不需要实现”的边界，也能为后续真正部署提供清单。

**实施内容**

- 新增 `服务器部署准备清单.md`。
- 文档覆盖部署前必须确认项：端口、HTTPS、`.env`、强密钥、登录、CORS、附件备份。
- 补充生产 `.env` 建议：
  - 基础服务
  - 登录与权限
  - AI / OCR / LlamaIndex
  - 附件存储
- 补充首次部署流程：安装依赖、准备 `.env`、初始化数据库、构建前端、启动服务、启动 AI worker、配置反向代理。
- 补充上线前检查命令：`check:system`、`smoke`、附件、MCP、LlamaIndex、索引队列等。
- 补充备份与恢复命令，强调恢复会覆盖数据，必须先 dry-run。
- 补充 OpenClaw 接入前置条件：HTTPS、登录权限、强 token、审批队列。
- 明确暂不实现项：当前阶段不做登录页面、多用户角色、页面恢复按钮和公网 MCP 写入。

**验证结果**

- 文档扫描：`服务器部署准备清单.md`、`优化执行记录.md`、`优化方案.md` 未发现目标英文占位、旧附件删除误导文案或典型 mojibake 字符。

**下一阶段建议**

当前优化方案的主要功能项已基本覆盖。后续建议进入人工验收：逐页在浏览器中检查今日、看板、笔记、汇总、智能检索、附件、回收站、系统页，并根据实际使用手感再排下一轮小修。
